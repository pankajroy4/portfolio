<div class="background">
  <div class="shape"></div>
  <div class="shape"></div>
</div>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "glass-form" }, data: {controller: "focus-input"}) do |f| %>
  <%= link_to :back, class: "back-arrow" do %>
    <i class="fas fa-arrow-left"></i>
  <% end %>
  <h3>Forgot Your Password?</h3>
  <%= render "admin/users/shared/error_messages", resource: resource %>

  <%= f.label :email, "Email", class: "label" %>
  <%= f.email_field :email, placeholder: "Your Email", class: "input-field", autofocus: false, autocomplete: "email" %>

  <%= f.submit "Send Reset Instructions", class: "submit-button" %>
  <%= render "admin/users/shared/links" %>
<% end %>

